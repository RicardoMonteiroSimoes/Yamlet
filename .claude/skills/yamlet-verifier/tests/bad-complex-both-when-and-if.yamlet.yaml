# pattern: complex with BOTH when AND if — must have exactly one
system: test-service
topic: Test Service
summary: A test service
description: A test.
blast_radius: low
front: internal
requirements:
- id: RQ-1
  description: Test complex with both when and if
  acceptance-criteria:
  - id: AC-1
    pattern: complex
    while:
    - the system is active
    when: a request arrives
    if: an error occurs
    shall:
    - handle the situation
